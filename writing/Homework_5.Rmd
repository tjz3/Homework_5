---
title: "Homework_5"
author: "Tony Zbysinski"
date: "`r Sys.Date()`"
output: pdf_document
---

```{r include = FALSE}
# loading libraries
library(tidyverse)
library(knitr)
library(broom)
library(readr)
library(lubridate)
```

```{r include = FALSE}
# loading and cleaning data
balt_homicidesv1 <- read.csv("../data/homicide.csv") %>%
  mutate(city_name = paste(city, state, sep = ", ")) %>%
  filter(city_name == "Baltimore, MD") %>%
  mutate(date = ymd(reported_date))
balt_homicidesv2 <- balt_homicides %>%
  mutate(month_year = format(as.Date(balt_homicides$date), "%Y-%m")) %>%
  mutate(month = month(date)) %>%
  mutate(winter_month = month %in% c(1:4, 11, 12)) %>%
  group_by(month_year) %>%
  mutate(total_homicides = summarize(count(uid)))
```

```{r include = FALSE}
# creating the graph

```

